# Shopping-App-Built as part of NEXUS Project 

# React based 
 deployed link: https://shopping-pixel-cart.vercel.app/

  ### 📁 Directory Tree Overview
    New proje/
    ├── public/
    │   └── logo.jpg
    ├── src/
    │   ├── api/                  # API client & fetch functions
    │   ├── components/           # Reusable UI components by feature
    │   │   ├── cart/             # Cart items, summary, & checkout modals
    │   │   ├── home/             # Hero banner, category grid, featured list
    │   │   ├── layout/           # Navbar, footer, announcement bar
    │   │   ├── product/          # Product display cards, grids, images & details
    │   │   ├── shop/             # Filters, search bar, sort selectors
    │   │   └── ui/               # Base design system primitives (Button, Modal, Badge,
  Skeleton)'
  
    │   ├── constants/            # Route names, navigation links, coupon codes
    │   ├── context/              # Global React Contexts & state reducers
    │   ├── hooks/                # Custom React hooks
    │   ├── pages/                # Page route components
    │   ├── styles/               # Global & modular CSS files
    │   ├── utils/                # Helper functions (storage, formatters)
    │   ├── App.jsx               # Application routes & provider wrappers
    │   └── main.jsx              # React DOM entry point
    ├── index.html                # Single-page application HTML template
    ├── package.json              # Dependencies & build scripts
 


